{
  "name": "@enschedule/docs",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "mint dev",
    "build": "mint validate",
    "validate": "mint validate",
    "links": "mint broken-links --check-anchors",
    "a11y": "mint a11y",
    "openapi:generate": "bun ./scripts/generate-openapi.ts",
    "typecheck": "mint validate",
    "lint": "mint validate"
  },
  "dependencies": {
    "@enschedule/types": "workspace:*",
    "@richie-rpc/openapi": "2.0.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "mint": "^4.2.754",
    "typescript": "^5.9.3"
  },
  "engines": {
    "bun": ">=1.3.0",
    "node": ">=20.17.0"
  }
}
