apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "enschedule.fullname" . }}-test-connection"
  labels:
    {{- include "enschedule.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "enschedule.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
