{
  "name": "test-worker",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot worker.ts",
    "typecheck": "tsc --noEmit",
    "serve": "bun worker.ts",
    "lint": "tsc --noEmit",
    "test": "bun test",
    "seed": "bun migrate.ts"
  },
  "devDependencies": {
    "@enschedule/worker": "workspace:*",
    "@enschedule/types": "workspace:*",
    "@types/bun": "^1.3.14",
    "date-fns": "^4.1.0",
    "dotenv": "^16.3.1",
    "typescript": "^5.9.3",
    "zod": "^4.1.12"
  }
}
