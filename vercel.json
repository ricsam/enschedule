{
  "version": 2,
  "installCommand": "NODE_ENV=development pnpm install --shamefully-hoist --force",
  "buildCommand": "./release-package.js && NODE_ENV=production turbo build && mkdir -p api && cp tests/hub-test/dist/index.js api/index.js",
  "outputDirectory": "tests/hub-test/dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
