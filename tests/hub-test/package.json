{
  "name": "@enschedule/hub-test",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot index.ts",
    "build": "bun build ./index.ts --outfile ./dist/index.js --target bun --format esm --external @enschedule/hub",
    "serve": "bun index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@enschedule/hub": "workspace:*",
    "@enschedule/types": "workspace:*",
    "dotenv": "^16.3.1",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.9.3"
  }
}
