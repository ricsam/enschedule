{
  "name": "@enschedule/pg-driver",
  "version": "0.0.0",
  "main": "./index.ts",
  "types": "./index.ts",
  "license": "MIT",
  "scripts": {
    "lint": "eslint .",
    "test": "jest",
    "build": "tsc -p ./tsconfig.json",
    "create-db": "PGPASSWORD=postgres dropdb  -h localhost -U postgres -p 6543 dev || true && PGPASSWORD=postgres psql -h localhost -U postgres -d postgres -p 6543 -c 'create database dev;'",
    "seed": "yarn run create-db && ts-node --project ./tsconfig.json -e 'import * as dotenv from \"dotenv\"; dotenv.config(); import { migrate } from \"./app/migrate\"; migrate();'"
  },
  "files": ["index.ts", "backend.ts", "log.ts", "dist"],
  "dependencies": {
    "cron-parser": "^4.6.0",
    "pg": "^8.8.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.25.5",
    "ts-jest": "^29.1.1",
    "zod": "^3.22.2",
    "debug": "^4.3.4"
  },
  "devDependencies": {
    "@types/jest": "^29.5.4",
    "@types/debug": "^4.1.8",
    "@enschedule/types": "workspace:*",
    "jest": "^29.6.3",
    "typescript": "^4.9.4",
    "@turbo/gen": "^1.10.12",
    "@types/node": "^20.5.2",
    "@types/pg": "^8.6.5",
    "eslint-config-custom": "workspace:*",
    "tsconfig": "workspace:*"
  }
}
